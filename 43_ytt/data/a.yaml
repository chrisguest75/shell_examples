#@ load("@ytt:data", "data")

---
version: 2.1

plan: &plan
  resource_class: small
  docker:
    - image: #@ data.values.terraform_image
  steps:
    - checkout
    - run:
        name: hello
        command: echo "hello world"

